<!DOCTYPE html>
<html>
  <head>
    <title>ТСН "Столетовский" — учёт электроэнергии</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'style', media: 'all', 'data-turbo-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload', defer: true %>
  </head>

  <body>
    <header>
      <h1>ТСН ЖСТИЗ "Столетовский"</h1>
      <nav>
        <% if user_signed_in? %>
          <%# <%= link_to 'Выход', destroy_user_session_path, method: :delete, data: { confirm: "Вы уверены, что хотите выйти?" } %> %>
          <%= button_to 'Выход', destroy_user_session_path, method: :delete, data: { confirm: "Вы уверены, что хотите выйти?" }, class: "logout-button" %>
        <% else %>
          <%= link_to 'Вход', new_user_session_path %> |
          <%= link_to 'Регистрация', new_user_registration_path %>
        <% end %>
      </nav>
    </header>

    <main>
      <% if notice %>
        <div class="notice"><%= notice %></div>
      <% end %>

      <% if alert %>
        <div class="alert"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>

    <footer>
      &copy; <%= Time.current.year %> ТСН "Столетовский". Все права защищены.
    </footer>
  </body>
</html>
